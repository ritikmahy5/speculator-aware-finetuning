torch>=2.2.0
transformers>=4.44.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.33.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.1.0
pyyaml>=6.0
scipy>=1.11.0
# Optional dependencies (uncomment as needed)
# vllm>=0.6.0          # for wall-clock speedup measurement
# lm-eval>=0.4.0       # for task evaluation
# wandb                 # for experiment tracking
# flash-attn>=2.6.0    # for faster attention
